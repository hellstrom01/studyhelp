[project]
name = "studyhelp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "fsrs>=4.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = ["pytest", "httpx"]
